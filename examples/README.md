# Examples

Examples are redacted documentation aids only. Use `.example` suffixes for any future configuration or secret template, and replace every placeholder privately at deployment time. Never commit a real domain, address, token, password, API key, kubeconfig, certificate, or debrid credential.

The current [`.env.example`](.env.example) contains no application variables because the upstream configuration contracts have not yet been verified.
