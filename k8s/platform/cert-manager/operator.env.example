# Copy this file to operator.env outside Git and replace all values.
# These values are configuration, not credentials, but the real file remains
# ignored so deployment-specific hostnames and contact details stay operator-managed.
ACME_EMAIL=operator@example.invalid
TLS_STAGING_HOST=cert-manager-staging.example.invalid
TLS_PRODUCTION_HOST=cert-manager.example.invalid
