# Authelia validation record — 2026-08-28

## Status

**BLOCKED — live Kubernetes access unavailable.**

This record separates repository and pinned-image proof from live deployment
claims. No Authelia workload, OIDC login, MFA enrollment, certificate, or
Remux `/admin` route was deployed or live-validated in this session.

## Local and source evidence

| Gate | Result |
| --- | --- |
| Official Authelia contract research | PASS — [`docs/research/authelia-current-contract-2026-08-28.md`](../research/authelia-current-contract-2026-08-28.md) |
| Pinned image | PASS — `ghcr.io/authelia/authelia:4.39.20`; local inspection resolved `sha256:b5f415d5f14b154c2aa2b186d9f329d879e223da36e115cd871db4c261d5af54` |
| Authelia renderer | PASS — private inputs remain outside the repository; placeholders are removed from rendered configuration/Ingress |
| AIOStreams renderer | PASS — existing 443/8443 URL checks plus OIDC input rendering |
| Remux renderer | PASS — main Ingress remains native and separate `/admin` Ingress renders the Authelia middleware |
| Kubernetes YAML/Kustomization static check | PASS — duplicate-key rejection and generated-file/resource reference checks for all three rendered bundles |
| Authelia image configuration validation | PASS — `v4.39.20 validate-config` accepted a disposable rendered bundle with generated Argon2/RSA/test-only material |
| Workspace safety | PASS — no private operator files, credentials, kubeconfig, or rendered Secret bundle added to Git |

## Live gates not verified

The following remain **NOT VERIFIED**, not PASS:

- authorized K3s context, namespace, Deployment, Service endpoint, local-path
  PVC, and production certificate;
- Authelia HTTPS `/api/health` and OIDC discovery through Traefik/TLS;
- valid and invalid password login, TOTP enrollment, two-factor login, and
  documented recovery;
- AIOStreams OIDC discovery, callback, admin session, local recovery login,
  and group-to-permission behavior;
- unauthenticated AIOStreams machine manifest behavior;
- anonymous Remux `/admin` protection and native Jellyfin bootstrap/login/API,
  playback, internal addon, and WebSocket behavior;
- Pod recreation with preserved Authelia identity, OIDC, and MFA state;
- any WAN/NAT or external reachability path.

## Blocking condition and next gate

The operator must first establish a safe, authorized Kubernetes access path
without reusing any exposed or rejected credential. Then render from protected
operator files, apply the three workload bundles in dependency order, run the
Authelia/AIOStreams/Remux verifiers, and record each browser and machine-path
result independently. Until that occurs, this milestone remains live-gate
blocked and WAN forwarding remains disabled.
