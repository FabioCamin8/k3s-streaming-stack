# Remux validation — 2026-08-28

This record is deliberately split between checks executed in the repository
workspace and checks that require the operator's K3s context. It contains no
credentials, private hostnames, or provider configuration.

## Contract basis

- Stable upstream release: `v0.27.0`.
- Image selected: `ghcr.io/lostb1t/remux:v0.27.0`.
- Contract record: [`docs/research/remux-current-contract-2026-08-28.md`](../research/remux-current-contract-2026-08-28.md).
- The immutable running image digest is **pending live deployment**; no digest
  is invented from a tag or from a registry lookup.

## Executed local checks

The following checks passed in this checkout:

- `bash -n` for both Remux operator scripts and both Remux verification scripts.
- `scripts/verify/remux-render.sh`: valid hostname rendering and placeholder
  rejection.
- Ruby YAML parsing for all five Remux resources.
- Manifest semantic assertions: one replica, `Recreate`, pinned image,
  container port 3000, `/data` mount, and no privileged container.
- The addon helper accepts a valid internal AIOStreams URL through its API
  phase and rejects an external URL before network access.
- `git diff --check`.

## Live checks pending

No Remux resource was applied from this workspace. The environment has no
`kubectl`, `kustomize`, or kubeconfig, and the known Proxmox SSH endpoint did
not accept an available key. No historical password was used. The following
must remain **pending**, not PASS, until an authorized operator context runs
them:

- Deployment, Service endpoint, local-path PVC, startup migration, and `/health`.
- Local/LAN Traefik route with correct SNI and the Remux certificate.
- Native Jellyfin bootstrap, authentication, catalog, playback-info, session,
  and WebSocket behavior.
- Internal Remux-to-AIOStreams metadata/configuration and provider-free path.
- Proxy and temporary-302 redirect behavior, including a direct-play external
  target and the hard `*.svc.cluster.local` leakage check.
- Pod recreation persistence, optional post-configuration node reboot, and
  `kubectl top pod`/`kubectl top node` observations.
- Physical Infuse or Swiftfin use; neither client is claimed as tested here.

The operator's WAN streaming forward is intentionally disabled for this
milestone. That is not a failure and is not required for the local/LAN route
checks. The historical AIOStreams external validation remains recorded in
[`docs/validation/aiostreams-2026-08-27.md`](aiostreams-2026-08-27.md) and is
not relabeled because the forwarding state later changed.

## Operator continuation

From a workstation with an authorized K3s context, render and apply the bundle
from [`k8s/remux/README.md`](../../k8s/remux/README.md), then run
`scripts/verify/remux.sh` with `REMUX_URL` and the protected
`REMUX_API_TOKEN_FILE`. Use its optional `REMUX_LOCATION_CHECK_URL` for each
tested direct-play stream. Record the resulting image digest and redacted
runtime output in this file before treating the Remux phase as complete.
