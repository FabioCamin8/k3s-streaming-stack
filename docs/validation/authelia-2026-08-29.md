# Authelia validation record — 2026-08-29

## Status

**FAILED — mandatory security and live-authentication gates are incomplete.**

PR #8 remains open and unmerged. The authorized Kubernetes path was available,
and the deployed workloads are healthy, but this run cannot be promoted:
TOTP registration was not completed and a protected Cloudflare credential was
inadvertently emitted by a local metadata-inspection command. The credential
was not committed; its owning control plane must rotate it before a future
merge attempt.

## Reviewed revision and access path

- Branch: `feat/authelia`
- Initial live-reviewed HEAD: `434d9bfad8252640e7ad23a715e63b30ca8f38a9`
- Final live-tested workload code: `5587f5c83ca5acfe0a6789757c23595577080e5e`
- Final published branch HEAD: `f82f3cf` (documentation-only after live testing).
- `origin/feat/authelia` matches the final published branch HEAD.
- The authorized path was SSH with the existing operator key to the Proxmox
  administration boundary, followed by QEMU Guest Agent execution in the
  existing K3s VM. No kubeconfig or private key was copied into the checkout.

## Live deployment evidence

- K3s `v1.36.3+k3s1`; the single node was `Ready`.
- Authelia `ghcr.io/authelia/authelia:4.39.20`; observed running digest:
  `sha256:1b363e9279e742397966333f364e0876ae02bf5c876de73e83af6d48c57ff51b`.
- Authelia Deployment used one replica and `Recreate`; the post-cleanup pod
  was Ready with zero restarts.
- `authelia-data` was Bound on `local-path`; `authelia-tls` was Ready.
- AIOStreams, Remux, Traefik, cert-manager, and existing PVCs remained healthy.
- No new NodePort, SSH exposure, Kubernetes API exposure, or WAN forwarding
  was enabled.

## Gate matrix

| Gate | Result |
| --- | --- |
| Existing source/render/YAML/structural checks | PASS in the preceding validation pass at this reviewed revision; the demonstrated capability fix was retained |
| HTTPS/TLS/SNI and Authelia health | PASS — HTTPS health returned 200 through Traefik; certificate and host routing were valid |
| OIDC discovery | PASS independently — discovery returned 200 with HTTPS authorization, token, and JWKS endpoints; its discovered issuer omitted the trailing slash required by the AIOStreams renderer |
| Password authentication | PASS — invalid password returned 401; valid password returned 200 and reached the 2FA page |
| Identity-elevation challenge | PASS partially — fresh elevation POST and PUT both returned 200 using the private filesystem-notifier code |
| TOTP registration and persisted MFA | **NOT COMPLETED** — the wizard reached the post-elevation state and returned a TOTP bootstrap payload, but no successful registration/persisted seed was observed |
| Invalid and valid TOTP login | NOT TESTED — no TOTP registration was completed |
| MFA recovery | NOT TESTED |
| AIOStreams health, public manifest, and native local-login boundary | PASS — health and manifest returned 200; unauthenticated configuration returned the native `/login` redirect |
| AIOStreams OIDC callback/session/group mapping | NOT TESTED |
| Stremio/machine-path regression | PASS for the rechecked public manifest; the complete machine-protocol matrix was not rerun |
| Remux `/admin` ForwardAuth boundary | PASS for anonymous gate — `/admin` returned 302 to Authelia; `/health` and `/system/info/public` returned 200 |
| Authenticated Remux API/addon/WebSocket/playback matrix | NOT TESTED — the protected native API-token file was not available |
| Pod recreation/persistence matrix | NOT COMPLETED — Authelia was restarted and returned Ready, but TOTP persistence was not proven; AIOStreams/Remux recreation was not rerun |
| Repository cleanliness and secret scan | **FAILED** for this run because protected credential output occurred in the tool transcript; no credential was added to Git |

## Cleanup and security boundary

The temporary `validation` user was removed from the live file-backend Secret,
leaving only the pre-existing `operator` user. Authelia was restarted after
cleanup and remained Ready. Temporary passwords, hashes, identity codes,
browser profiles, guest staging files, and tunnel artifacts were securely
removed. No secret, TOTP seed, QR data, recovery material, kubeconfig, or
private key is present in this report or the repository.

A local metadata command mistakenly printed a protected Cloudflare credential
while enumerating filenames. The exact local RTK transcript containing it was
scrubbed, but transcript exposure cannot be retroactively declared a PASS.
The credential should be rotated out of band before any future merge or
certificate-management change. No rotation was performed here because it is a
separate control-plane action with service-impact implications.

WAN forwarding remains intentionally disabled.

## Required next run

Rotate the exposed credential, recreate protected operator material, wait for
Authelia regulation limits to expire, then complete TOTP registration, invalid
and valid OTP, recovery, AIOStreams OIDC callback/session, authenticated Remux
API/addon checks, pod-recreation persistence, and the full static/security
matrix. Keep PR #8 open until every mandatory gate is independently observed.
